# Copiá este archivo como .env.local para desarrollo local
# En Netlify: Site settings → Environment variables

# PIN compartido entre Hernán y Sofía
VITE_APP_PIN=TESTING

# Gemini API key — server-side only (Netlify Function). Free tier: aistudio.google.com/apikey
GEMINI_API_KEY=
# Opcional: override del modelo (default gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
